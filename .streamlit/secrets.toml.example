# 复制本文件为 .streamlit/secrets.toml 并填入真实值。
# .streamlit/secrets.toml 已被 .gitignore 排除，不会被提交到仓库。

# 用于加密浏览器登录 Cookie 的密钥，请替换为任意长随机字符串
cookie_password = "请替换为一段足够长的随机字符串"

# 多用户账号密码表（键=账号，值=密码），可配置多行
[users]
admin = "请替换为密码"
